# Corpus entries where `ruta -p` and `luac -p` are expected to disagree.
#
# The scoreboard reads this file so that it can tell a known gap from a regression: an entry
# listed here and still failing is expected, an entry listed here that starts passing should
# be removed, and a mismatch that is *not* listed here is a bug.
#
# 0 entries, against a corpus of 602. The last one came off at stage 4F, when code generation
# gained the limits the reference reports from its parser.

[cases]
