[workspace]
resolver = "3"
members = ["crates/*", "xtask"]

[workspace.package]
version = "0.0.0"
edition = "2024"
license = "MIT"
repository = "https://github.com/CloudHolic/ruta"

[workspace.dependencies]
ruta-bytecode = { path = "crates/ruta-bytecode" }
ruta-compile = { path = "crates/ruta-compile" }
ruta-conformance = { path = "crates/ruta-conformance" }
ruta-runtime = { path = "crates/ruta-runtime" }
ruta-syntax = { path = "crates/ruta-syntax" }

anyhow = "1"
cc = "1"
clap = { version = "4", features = ["derive"] }
serde = { version = "1", features = ["derive"] }
toml = "1"

[workspace.lints.rust]
unsafe_code = "deny"
missing_debug_implementations = "warn"
unreachable_pub = "warn"

[workspace.lints.clippy]
all = { level = "warn", priority = -1 }

[profile.release]
lto = "thin"
codegen-units = 1
